{
  "name": "@yaatal/gatekeeper-yaatal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "echo \"run 'pnpm run-local' in the root directory instead\" >&2 && exit 1",
    "types:generate": "wrangler types && node fix-worker-types.mjs",
    "types:check": "tsc --noEmit",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@gadgets/workshop-shared": "workspace:*",
    "capnweb": "catalog:",
    "capnweb-validate": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
