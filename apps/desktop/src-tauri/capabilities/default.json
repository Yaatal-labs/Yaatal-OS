{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Deny-by-default shell permissions scoped to the two first-party windows.",
  "windows": ["sell", "shop"],
  "permissions": ["core:default"]
}
