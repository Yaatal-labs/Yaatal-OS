{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Deny-by-default shell permissions for the unified first-party window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}